export class Broker{
    name: String;
    startDate: Date;
    endDate:Date;
    constructor(
        name: String,
        startDate:Date,
        endDate:Date
    ) {
       this.name = name;
       this.startDate = startDate;
       this.endDate = endDate;
        }
  
}